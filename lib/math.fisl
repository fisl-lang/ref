


label math::mul
    pull addr

    pull a
    pull b

    let o be 0

    label math::mul::loop
        let o be o plus a 
        let b be b minus 1
    if b greater 0 goto math::mul::loop

    push o

    push addr
    return



